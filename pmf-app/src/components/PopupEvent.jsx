// Rendered as a component, not its own route — a popup announcement
// isn't something that should be reachable by URL.
function PopupEvent() {
  return (
    <div>
      <p>Popup Event</p>
    </div>
  )
}

export default PopupEvent
